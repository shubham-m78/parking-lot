spring:
  datasource:
    url: jdbc:h2:mem:parkingdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 880265312433-lm65l0beci6ei2fuss175amkrfotia45.apps.googleusercontent.com
            client-secret: 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
            redirect-uri: "http://localhost:8080/login/oauth2/code/google"
            client-name: Google
            scope:
              - openid
              - email
              - profile
        provider:
          google:
            authorization-uri: https://accounts.google.com/o/oauth2/v2/auth
            token-uri: https://www.googleapis.com/oauth2/v4/token
            user-info-uri: https://www.googleapis.com/oauth2/v3/userinfo
            user-name-attribute: sub
      resourceserver:
        jwt:
          # Validate Google-signed JWTs using Google's JWKs
          jwk-set-uri: https://www.googleapis.com/oauth2/v3/certs

server:
  port: 8080

app:
  security:
    admin-emails:
      - shubhammotghare78@gmail.com
    user-emails:
      - user@example.com

logging:
  level:
    org.springframework.boot.context.properties: DEBUG